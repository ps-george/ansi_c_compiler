#include "../lexer/lexer_main.cpp"



int main() {
  // Lex the input, output is JSON
  
  
  // Take the output from the lexer and parse it
  
  // Take the output from the parser and perform necessary functions to compile it
  
}
